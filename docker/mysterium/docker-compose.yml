services:
  myst:
    image: mysteriumnetwork/myst:latest
    container_name: myst
    cap_add:
      - NET_ADMIN
    network_mode: host
    ports:
      - "4449:4449"
    volumes:
      - /srv/mysterium/mysterium-node:/var/lib/mysterium-node
    restart: unless-stopped
    command: ["service", "--agreed-terms-and-conditions"]